# EFS挂载路径配置
EFS_MOUNT_PATH=/mnt/efs
HEALTH_CHECK_DIR=/mnt/efs/test

# EFS DNS端点（用于问题诊断）
EFS_DNS=fs-xxxxxxxx.efs.region.amazonaws.com

# 日志配置
LOG_DIR=/var/log/efs-check
CLOUDWATCH_LOG_GROUP=/ecs/efs-check

# 应用配置
PORT=5000
DEBUG=False 